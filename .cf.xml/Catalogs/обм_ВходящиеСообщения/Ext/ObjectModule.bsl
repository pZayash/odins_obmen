
Процедура ПередЗаписью(Отказ)

	общ.СправочникОбъект_УстановитьУИДкакКод(ЭтотОбъект);
	
	общ.ЕслиПустое(ДатаВремя, общ.ДатаУниверсальная());
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	
	Если ДополнительныеСвойства.Свойство("ЭтоНовый") И ДополнительныеСвойства.ЭтоНовый = Истина Тогда
		Справочники.обм_ВходящиеСообщения.Статус_Установить( // Получен
			ЭтотОбъект.Ссылка, 
			Перечисления.обм_ВходящиеСообщения_Статусы.Получен
			);
		
	КонецЕсли;
	
КонецПроцедуры
