&НаКлиенте
Процедура ОбработкаКоманды(пСсылка, ПараметрыВыполненияКоманды) 

	параметрыФормы = Новый Структура("Ссылка", пСсылка);
	
	ОткрытьФорму(
		"РегистрСведений.обм_Измененные_ПравилаАдресации.ФормаСписка", 
		параметрыФормы,
		, // Владелец
		пСсылка
		);

КонецПроцедуры 

