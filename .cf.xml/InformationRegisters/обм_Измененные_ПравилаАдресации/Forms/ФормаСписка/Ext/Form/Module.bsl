
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)  
	
	ссылкаИзмененного = Неопределено;
	Если Параметры.Свойство("Ссылка", ссылкаИзмененного) Тогда
		общ.СКД_Отбор_Установить(Список.Отбор, "Ссылка", ссылкаИзмененного);
		ЭтаФорма.Заголовок = СтрШаблон("Правила адресации для '%1'", ссылкаИзмененного); 
		
	КонецЕсли;

КонецПроцедуры