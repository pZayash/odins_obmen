Процедура ПередЗаписью(Отказ, Замещение)
		
	// Регистр не предназначен для обмена, он сам часть подсистемы обмена
	// Поэтому записывать в режиме ОбменДанными.Загрузка
	// Чтобы не срабатывали сторонние подписки и не вызывали задержку
	ЭтотОбъект.ОбменДанными.Загрузка = Истина;


КонецПроцедуры

